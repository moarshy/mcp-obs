// Re-export oRPC configurations for easy importing
export * from './router'

// Server actions are imported directly from ./actions/ subdirectory
// Example: import { createMcpServerAction } from '@/lib/orpc/actions/mcp-servers'

// Client utilities should be imported directly when needed
// Example: import { useServerAction } from '@orpc/react/hooks'