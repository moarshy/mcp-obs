{
  "mcpServers": {
    "demo-mcp2": {
      "description": "Demo MCP server with echo tool that responds 'hello from mcp-obs (OAuth Streamable HTTP): {message} (authenticated as: user@example.com)' - requires OAuth server running and valid Bearer tokens",
      "url": "http://localhost:3005/mcp"
    }
  }
}