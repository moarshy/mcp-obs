{
  "name": "mcp-obs",
  "version": "0.1.0",
  "private": true,
  "description": "mcp-obs - Observability and management platform for MCP servers",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:tunnel": "bun run scripts/ngrok.ts",
    "build": "bun run build:packages && cd packages/dashboard && bun run build",
    "build:packages": "cd packages/database && bun run build && cd ../server-sdk && bun run build && cd ../client-sdk && bun run build",
    "lint": "cd packages/dashboard && bun run lint",
    "type-check": "cd packages/database && bun run type-check && cd ../server-sdk && bun run type-check && cd ../client-sdk && bun run type-check && cd ../dashboard && bun run type-check",
    "clean": "bun run clean:packages && cd packages/dashboard && bun run clean",
    "clean:packages": "rm -rf packages/*/dist packages/*/build packages/*/.next",
    "db:generate": "cd packages/dashboard && bun run db:generate",
    "db:migrate": "cd packages/dashboard && bun run db:migrate",
    "studio": "cd packages/database && bun run db:studio",
    "db:push": "cd packages/database && bun run db:push",
    "dev": "sst dev"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/aws-lambda": "8.10.152",
    "@types/bun": "latest",
    "@types/nunjucks": "^3.2.6",
    "@types/prop-types": "^15.7.15"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.5.1",
    "sst": "^3.17.14"
  },
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "packageManager": "bun@1.0.0"
}